spring:
  application:
    name: merchantapi
#  profiles:
#    active: development
  cloud:
    config:
      uri: http://localhost:8888

  rabbitmq:
        addresses: amqp://localhost:5672
        username: alan
        password: alan

